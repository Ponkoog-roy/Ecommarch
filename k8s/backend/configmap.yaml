apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: roy-food-ordering
data:
  # Replace with your RDS endpoint once created, e.g.
  # roy-db.abcdefg123.us-east-1.rds.amazonaws.com
  DB_HOST: "REPLACE_WITH_RDS_ENDPOINT"
  DB_PORT: "5432"
  DB_NAME: "roydb"
  PORT: "4000"
  NODE_ENV: "production"
